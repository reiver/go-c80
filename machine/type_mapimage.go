package c80machine

import (
	"image"
)

func (receiver *Type) MapImage(x int, y int, width int, height int) image.Image {
	if nil == receiver {
		return nil
	}

panic("TODO: c80/machine.Type.MapImage()")
}
